# Integrated technical baseline — 2026-09-15

Code commit: `183b96a6eefb1e73d6ed27e553f9d3bc139e59e8`. The baseline tag also includes repository navigation and version-management documentation.

Validation used the CERN LCG environment referenced by `envset.sh`, a separate build directory and the exact committed source files. This is a technical regression baseline, not a complete detector calibration or a new physics-performance measurement.

- Python analysis tests: 9 passed (SPE response, timing metrics, photon thinning).
- Python run-validator precision test: 1 passed.
- Full top-level CMake configure/build: passed, including the production simulation.
- Standalone optical boundary, prototype geometry, primary generator, sensor response and assembly-scan targets: built successfully.
- Primary generator requests/intersections/spreads: passed.
- Assembly scan at 0 and 29.5 mm in both LG/noLG modes: passed with explicit scan environment.
- Optical boundary reflect/absorb/transmit/mixed fixtures: 500 photons each, seed 42, passed.
- WLS and missing-index diagnostic budget fixtures: 100 events each, seed 42, passed their expected diagnostic assertions; lifecycle fixture passed. A missing-index fixture intentionally exercises an error classification.
- Nominal interface/navigation checks: passed for both LG/noLG modes.
- Conditional sensor response fixture: 500 photons, 450 nm, 45 degrees, wafer start, seed 42, passed in both modes.

The assembly fixture requires `CBDsim_PROTO_SCAN_S_MM`; its initial invocation omitted that environment variable. Corrected invocations above passed. No production physics code was changed for this invocation correction.

The existing sensor/readout idealizations remain: conditional wafer QE, ideal SPE and no complete PMT/electronics calibration. These checks do not establish statistical significance for a timing-performance ranking. Historical plots remain associated with their original model and analysis conditions.
